# Intentionally-entropic PUBLIC files (A's c18 ask: dogfood secretgate .secretgateignore).
# proofs/ = signed release receipts: the base64 payload + 65-byte signature are
# the point of the file, and every one is re-verified in CI, never consumed as a
# credential. selftest.yml = the CI parity harness, whose fixtures legitimately
# embed 65-byte signatures and sample addresses (also CI-executed, asserted).
proofs/
.github/workflows/selftest.yml
